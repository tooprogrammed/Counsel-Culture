# Cousel-Culture

# Terminal 1 — LiveKit media server
cd livekit
.\livekit-server.exe --config livekit.yaml

# Terminal 2 — token server
cd server
npm run dev

# Terminal 3 — client
cd client
npm run dev